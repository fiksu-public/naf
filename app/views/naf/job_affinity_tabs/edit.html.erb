<div id='naf'>
  <%= render :partial => "layouts/naf/application" %>
  <div id="record">
    <%= link_to 'Back', controller => 'job_affinity_tabs', :action => 'show', :id => @tab.id, :job_id => @job.id %>

    <h3 id="record_name">Editing Job Affinity Tab</h3>

    <%= render 'form' %>
  </div>
</div>
