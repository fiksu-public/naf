<%= form_for(@run_group) do |f| %>
  
  <% if @run_group.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@run_group.errors.count, "error") %> prohibited Application Run Group from being saved:</h2>
    
    <ul>
      <% @run_group.errors.full_messages.each do |msg| %> 
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :application_run_group_name %>
  </div>

  <br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
