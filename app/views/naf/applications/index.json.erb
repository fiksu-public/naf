<%
  rows = @applications.each do |application|
    application.insert(6, last_queued_at_link(Naf::Application.find(application.first)))
    application[8] = link_to image_tag('control_play_blue.png', :class => 'action', :title => 'Enqueue Now'), "#", {:class => "enqueue", :id => application[0]}
  end
%>
<%= raw rows %>