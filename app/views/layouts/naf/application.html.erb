<!DOCTYPE html>
<html>
<head>
  <title>Fiksu&trade; | Job Scheduling System</title>
  <%= stylesheet_link_tag    "naf" %>
  <%= javascript_include_tag "naf" %>
  <%= csrf_meta_tags %>
</head>
<body>

<table width="100%" border="0" cellspacing="0">

  <tr>
    <td colspan="2" id="header">
      <h3>Fiksu&trade; | Job Scheduling System</h3>
       <ul id="tabs">
      <% tabs.each do |tab| %>
        <% if highlight_tab?(tab) %>
          <li id="on">
        <% else %>
          <li>
        <% end %>

          <%= generate_index_link(tab) %>

        </li>
     <% end %>
     </ul>
       
    </td>
  </tr>

  <tr valign="top">
    <td id="main">
      <div id="status"></div>
      <% if current_page?(naf.root_url) or current_page?(:action => 'index') %>
        <span id="create">  <%= generate_create_link %> </span>
      <% end %>
      <% if display_job_search_link? %>
        <span id="job_search"> <%= link_to "Search for Jobs", "#", {:class => "job_search"} %> </span>
        <p class="clear"></p>
      <% elsif nested_resource_index? %>
        <span id="parent"> <%= parent_resource_link %> </span>
        <p class="clear"></p>
      <% end %>
      <%= yield %>
    </td>
  </tr>

  <tr>
    <td colspan="2" id="footer">
    <%= link_to "Back to the main Rails Application", "/" %>
    </td>

  </tr>

</table>





</body>
</html>
