<div id="tooltip"></div>
<table width="100%" border="0" cellspacing="0" id="naf">

  <tr>
    <td colspan="2" id="header">
      <h3><%= Naf.title.html_safe %></h3>
      <ul id="tabs">
        <% tabs.each do |tab| %>
            <% if highlight_tab?(tab) %>
                <li id="on">
            <% else %>
                <li>
            <% end %>

            <%= generate_index_link(tab) %>

            </li>
        <% end %>
      </ul>

    </td>
  </tr>

  <tr valign="top">
    <td id="main">
      <% if current_page?(naf.root_url) or current_page?(:action => 'index') %>
          <span id="create">  <%= generate_create_link %> </span>
      <% end %>
      <% if display_job_search_link? %>
          <span id="job_search"> <%= link_to "Search for Jobs", "#", { :class => "job_search" } %> </span>

          <p class="clear"></p>
      <% elsif nested_resource_index? %>
          <span id="parent"> <%= parent_resource_link %> </span>

          <p class="clear"></p>
      <% end %>
      <div id="status"></div>
    </td>
  </tr>
</table>
